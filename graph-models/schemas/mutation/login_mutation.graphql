extend type Mutation {
    login(input: LoginInput!): LoginResult! @goField(forceResolver: true)
}

# Login ...
input LoginInput {
    email: String!
    password: String!
}
union LoginResult =
    | AdvancedError
    | Successful
    | TokenPair