extend type Query {
    room_messages(room_id: ID, filter: MessageFilter, params: Params): RoomMessagesResult @goField(forceResolver: true)

}

# RoomMessages ...
input MessageFilter {
    text_fragment: String
    author_id: ID
    chat_id: ID
    room_id: ID
}
type MessageArray {
    messages: [Message!]
}
union RoomMessagesResult =
    | AdvancedError
    | MessageArray
