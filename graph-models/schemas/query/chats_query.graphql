extend type Query {
    chats(name_fragment: String!, params: Params): ChatsResult @goField(forceResolver: true)

}

# Chats ...
type ChatArray {
    chats: [Chat!]
}
union ChatsResult =
    | AdvancedError
    | ChatArray
